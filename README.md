# multimodal_semantic_image_search
A CLIP-based text-to-image retrieval system that uses vision–language embeddings and vector search (Chroma) to fetch relevant images from large corpora. Includes metadata-aware indexing, evaluation &amp; reranking pipelines, and outperforms keyword baselines on ambiguous multi-attribute queries.
